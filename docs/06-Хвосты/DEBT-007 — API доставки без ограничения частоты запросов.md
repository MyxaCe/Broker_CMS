---
title: DEBT-007 — Публичная дверь открыта без ограничения частоты запросов
aliases: [DEBT-007]
type: debt
status: paid
priority: p1
created: 2026-08-03
updated: 2026-08-04
pay_by: M1
related: ["[[ADR-0019]]", "[[ADR-0018]]", "[[ADR-0016]]", "[[ADR-0020]]"]
tags: [debt, status/paid, p1, stage/M1, area/delivery, area/security]
---

# DEBT-007 — API доставки без ограничения частоты запросов

> [!success] Карточка
> **Статус:** 🟢 погашен `2026-08-04`, прожил около часа · **Приоритет:** p1
> **Где:** `src/modules/delivery/api` · **Решение:** [[ADR-0020]]

## Как должно быть

Ключ доставки имеет предел частоты запросов (ТЗ разд. 6). Утёкший ключ ограничен в том, что им можно сделать, а один сорвавшийся потребитель не роняет выдачу остальным.

## Как сделано сейчас

Ограничения нет ни на уровне приложения, ни на уровне прокси: каждый запрос доходит до базы и читает канал и релиз.

## Почему так

Ограничение частоты — это состояние, разделяемое между процессами; в памяти одного процесса оно бессмысленно, как только процессов станет два. Настоящая реализация опирается на Redis, который в контуре есть, но к доставке ещё не подключён.

Делать «пока в памяти» я не стал намеренно: такая версия выглядит как защита, не являясь ею, и её оставляют.

## Чем это грозит

> [!danger] Утёкший ключ ничем не ограничен
> Ключ, попавший в чужие руки, работает с любой частотой, пока его не отозвали. Отзыв мгновенный, но заметить утечку без ограничения частоты не по чему: аномальная нагрузка — первый признак.

Второе: каждый запрос доходит до БД, поэтому обычный перебор упирается в базу, а не в счётчик.

## Как погашен

Условие «до того как дверь окажется доступна извне» соблюдено: хвост прожил около часа и был закрыт до какой-либо выкатки. Решение — [[ADR-0020]].

- [x] предел на ключ, а не на адрес;
- [x] счётчик в Redis, общий для всех процессов — проверено двумя экземплярами ограничителя, видящими один счётчик;
- [x] превышение отвечает `429` с `Retry-After`; в перечень кодов добавлен `rate-limited`;
- [x] отдельный предел на **неудачные** авторизации, причём отказ наступает до обращения к базе;
- [ ] **предел настраивается на ключ** — пределы пока константы в коде. Разным потребителям нужны разные профили; переносится в [[DEBT-008]];
- [ ] грубый предел на прокси ([[ADR-0016]]) как второй рубеж — вместе с настройкой прокси.

Сверх изначального списка появилось решение о поведении при недоступном счётчике: чтение пропускается, подбор закрывается. Обоснование — в [[ADR-0020]].

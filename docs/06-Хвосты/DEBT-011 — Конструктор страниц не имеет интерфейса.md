---
title: DEBT-011 — Дерево блоков редактируется как поле JSON, конструктора для редактора нет
aliases: [DEBT-011]
type: debt
status: open
priority: p1
created: 2026-08-06
updated: 2026-08-06
pay_by: M3
related: ["[[ADR-0024]]", "[[Дорожная карта]]"]
tags: [debt, status/open, p1, stage/M3, area/design, area/process]
---

# DEBT-011 — Конструктор страниц не имеет интерфейса

> [!warning] Карточка
> **Статус:** open · **Приоритет:** p1 · **Когда платим:** M3, до приёмки этапа
> **Где:** `src/modules/design/pages` · **Связано:** [[ADR-0024]]

## Как должно быть

Редактор собирает страницу из блоков: выбирает тип из списка, задаёт вариант, заполняет пропсы, двигает блоки, видит предпросмотр. ТЗ 2.1 требует ещё и живого предпросмотра палитры при правке токена.

## Как сделано сейчас

Дерево блоков — поле JSON. Модель правильная, проверки работают: тип и вариант сверяются с реестром, стиль берёт значения только из токенов, вложенность ограничена, произвольный код закрыт для редактора.

Но собрать страницу в текстовом поле с JSON человек не может. Точнее — может разработчик, и не может тот, для кого это делается.

## Почему так

Сознательный порядок: сначала модель и правила, потом интерфейс. Обратный порядок означал бы конструктор поверх непроверенной модели — и переделку интерфейса при каждом изменении правил.

Второе: интерфейс — это самостоятельная работа на React с состоянием, перетаскиванием и предпросмотром. Смешивать её со сборкой доменной модели значило бы делать обе хуже.

## Чем это грозит

> [!danger] Это не косметика
> CMS, в которой редактор не может собрать страницу, не выполняет своей задачи, как бы хороша ни была модель под капотом. На приёмке этапа это первое, что заметят, — и справедливо.

Пока страниц нет ни на одном контуре, вреда нет. Но это единственная причина, по которой хвост ещё не критичен.

## Когда платим

M3, до приёмки этапа. Это самая большая оставшаяся часть M3.

- [ ] выбор типа блока из реестра с группировкой и поиском;
- [ ] форма пропсов, выведенная из описания типа, а не написанная руками для каждого;
- [ ] порядок блоков перетаскиванием, вложенность в разрешённые слоты;
- [ ] стиль блока — выпадающие списки токенов, а не ввод строк: перечни уже закрытые, интерфейс обязан это показывать;
- [ ] расхождения из `validateBlockTree` показываются прямо у блока, а не общим списком внизу;
- [ ] живой предпросмотр палитры при правке токена (ТЗ 2.1);
- [ ] предпросмотр страницы по preview-токену (ТЗ 5.4);
- [ ] выбор переиспользуемой секции по ключу — с показом того, чья она: своя или унаследованная от бренда;
- [ ] редактор меню деревом (ТЗ 2.2 прямо требует «свой редактор»): перетаскивание пунктов, выбор страницы из списка, а не ввод идентификатора.

> [!note] Дополнено `2026-08-06`
> Модель навигации и секций собрана и проверена ([[ADR-0026]]), но правятся они тем же полем JSON, что и блоки страницы. Это тот же хвост, а не новый: интерфейс один — конструктор — и делать его по частям означало бы трижды переписать одно и то же состояние.

---
title: Хвосты (индекс)
aliases: [Хвосты, Технический долг, TECH-DEBT]
type: moc
updated: 2026-07-28
tags: [debt, meta]
---

# 🧵 Технический долг и «хвосты»

Всё, что сделано **не так, как должно быть в итоге**: заглушки, упрощения, отложенные проверки, временные обходы, «пока захардкожено».

Шаблон: [[Шаблон хвоста]]

> [!important] Железное правило
> Хвост записывается **в тот момент, когда он оставляется**, а не когда о нём вспомнили. В коде рядом ставится `// TODO(DEBT-XXX): краткая причина` — так хвост находится с обеих сторон: из кода в документацию и обратно.

> [!danger] Ограничение регулируемого домена
> Хвост, затрагивающий безопасность (разд. 6 ТЗ), комплаенс (2.4), изоляцию тенантов (разд. 3) или валидацию выдачи:
> - не может иметь приоритет ниже `p1`;
> - не переводится в `accepted-permanently` без явного согласия заказчика, зафиксированного отдельным ADR.

---

## Открытые

```dataview
TABLE WITHOUT ID
  link(file.link, aliases[0]) AS ID, title AS Упрощение, priority AS Приоритет, pay_by AS "Платим на", updated AS Обновлён
FROM #debt
WHERE type = "debt" AND status = "open"
SORT priority ASC
```

| ID | Упрощение | Приоритет | Платим |
|---|---|---|---|
| [[DEBT-003]] | Переопределения версий не проверены на миграциях и сборке | p2 | при первом `migrate` и `build` |

---

## Погашенные

| ID | Упрощение | Погашен |
|---|---|---|
| [[DEBT-001]] | Lock-файл отсутствует, CI с `--frozen-lockfile` упадёт на установке | `2026-07-29`, прожил ~час |
| [[DEBT-002]] | Правила изоляции не подключены к коллекциям | `2026-07-29`, до создания первого тенанта |

```dataview
TABLE WITHOUT ID
  link(file.link, aliases[0]) AS ID, title AS Упрощение, updated AS Погашен
FROM #debt
WHERE status = "paid"
SORT updated DESC
```

---

## Принятые навсегда

```dataview
TABLE WITHOUT ID
  link(file.link, aliases[0]) AS ID, title AS Ограничение, related AS Основание
FROM #debt
WHERE status = "accepted-permanently"
```

> [!note] Что здесь лежит
> Осознанные ограничения системы, с которыми решено жить. Каждое обязано ссылаться на ADR с согласием заказчика — иначе это не принятое ограничение, а забытый хвост.

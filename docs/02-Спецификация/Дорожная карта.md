---
title: Дорожная карта
aliases: [Дорожная карта, ROADMAP, Этапы]
type: spec
status: active
updated: 2026-08-06
related: ["[[Критерии приёмки]]", "[[Задачи (индекс)]]"]
tags: [spec, area/process]
---

# 🛣️ Дорожная карта (M0–M6)

Источник — разд. 12 ТЗ. Детализация задач появляется при входе в этап, чтобы не плодить мёртвые записи.

| Этап | Статус | Содержание |
|---|---|---|
| **pre-M0** | ✅ done `2026-07-28` | Документация, ответы на [[Q-001]]…[[Q-003]], [[Параметры внедрения]] |
| **M0** | ✅ done `2026-08-03` | Каркас: тенанты, наследование, роли и доступ, аудит, окружение с zod, CI со всеми проверками |
| **M1** | ✅ done `2026-08-04` | Доставка + релизы: контракт, кеш, ETag, события через outbox+RabbitMQ, откат |
| **M2** | ✅ done `2026-08-05` | Часть 1 — поток: новости, видео, промо, таксономии, планировщик, ленты, поиск |
| **M3** | 🟡 in-progress `2026-08-06` | Часть 2 — токены, реестр блоков, конструктор страниц, навигация, SEO, комплаенс-ограничители, предпросмотр |
| **M4** | ⚪ not-started | Часть 4 — снапшот MDS, доступ к инструментам, тема и layout терминала, конфиг кабинета, реконсиляция |
| **M5** | ⚪ not-started | Воркфлоу с аппрувами, юрисдикции, A/B и гео-персонализация, DAM с картой использования |
| **M6** | ⚪ not-started | Миграция данных, двойная выдача, переезд потребителей, вывод legacy |

---

## Где мы на самом деле `2026-08-06`

**M0 закрыт.** Тенанты с наследованием, изоляция как правило доступа (доказана автотестом на живой БД), роли, журнал аудита с неизменяемостью на уровне Postgres, окружение через zod с fail-closed, CI из трёх заданий.

**M1 закрыт `2026-08-04`.** Единственное отложенное — серверный кеш выдачи, обоснование ниже.

| Часть | Состояние |
|---|---|
| Модель релиза, номера, снапшоты, каналы | ✅ [[ADR-0015]] |
| Сборка релиза через валидаторы, отчёт у провалов | ✅ |
| Транзакционный outbox, публикация с подтверждениями | ✅ [[ADR-0017]] |
| Публикация и откат переключением канала | ✅ |
| Ключи доставки со скоупами | ✅ [[ADR-0018]] |
| Контракт выдачи, схема как закон на выходе | ✅ [[ADR-0019]] |
| `ETag` и `304` | ✅ |
| Ограничение частоты | ✅ [[ADR-0020]] |
| Публикатор outbox отдельным процессом | ✅ `pnpm run worker:outbox` |
| Планировщик переходов потока | ✅ `pnpm run worker:schedule` |
| Структурный журнал с сокрытием секретов | ✅ |
| **Серверный кеш выдачи** | ⚪ осознанно отложен, см. ниже |

> [!note] Почему серверный кеш отложен
> Ключ кеша содержит идентификатор релиза, а релиз неизменяем — значит запись кеша **не требует инвалидации по содержимому**. Инвалидации требует только указатель канала, и правильный её источник — событие `cms.release.published.v1`, то есть потребитель шины, которого ещё нет.
>
> Пока ответ собирается из двух точечных запросов к БД, а объём выдачи — одна карточка настроек сайта. Кеш здесь дал бы выигрыш, несопоставимый с ценой преждевременной инвалидации. Возвращаемся к нему вместе с моделью страниц, когда появится что кешировать.

**M2 закрыт `2026-08-05`.** Поток целиком: материалы, видео с вычисляемым статусом эфира, промо, таксономии, медиа, планировщик переходов, ленты с курсорной пагинацией, RSS/Atom, полнотекстовый поиск по локали. Пять ресурсов выдачи наружу, каждый с ключом, пределом частоты и валидацией схемой.

Не сделано из части 1 намеренно: **импорт из внешних лент** — ТЗ относит его к M4+ и помечает как опциональный.

**M3 в работе.** Слой А — модель — собран; слой Б закрыт; остаётся слой В и интерфейс редактора.

| Часть | Состояние |
|---|---|
| Дизайн-токены: примитив → роль → токен компонента | ✅ [[ADR-0024]] |
| Контраст WCAG AA блокирует сборку | ✅ |
| Экспорт токенов в CSS и JSON | ✅ |
| Реестр блоков, закрытые перечни стиля, ограничение вложенности | ✅ |
| Страницы: путь, история путей, редиректы 301 | ✅ |
| Комплаенс-ограничители, гейт на сборке | ✅ [[ADR-0025]] |
| Переиспользуемые секции с наследованием | ✅ [[ADR-0026]] |
| Глобальные области в выдаче | ✅ ручка `bootstrap` |
| Навигация деревом со ссылками на страницы | ✅ [[ADR-0026]] |
| SEO: sitemap, robots, hreflang, JSON-LD, canonical | ✅ [[ADR-0027]] |
| Редиректы отдельной коллекцией, проверка циклов | ✅ [[ADR-0027]] |
| **Конструктор страниц и редактор меню** | ❌ [[DEBT-011]] |
| **Предпросмотр по preview-токену** | ⚪ вместе с конструктором |

> [!note] Модель части 2 закрыта `2026-08-07`
> Всё, что описывает ТЗ в слоях А, Б и В, собрано и проверено. Незакрытым остаётся интерфейс редактора — и это единственное, что отделяет M3 от приёмки.

## Задачи по этапам

```dataview
TABLE WITHOUT ID
  link(file.link, aliases[0]) AS ID, title AS Задача, status AS Статус, priority AS Приоритет
FROM #task
WHERE status != "done" AND status != "cancelled"
GROUP BY stage AS Этап
SORT priority ASC
```

---

## Порядок этапов — не произвольный

> [!important] Почему нельзя переставлять
> Изоляция тенантов, аудит и fail-closed-окружение (M0) должны существовать **до** появления данных и выдачи. Ретроспективно навесить изоляцию на существующие данные в регулируемом домене — это миграция с доказательством, что утечки не было. Перестановка этапов — ADR, а не рабочее решение по ходу.

> [!warning] Зависимость, о которой ТЗ предупреждает отдельно
> **A/B-варианты (M5) обязаны быть учтены в модели страницы и доставке уже на M1/M3**, если ответ на [[Q-002]] — «нужны». Способ учесть их дёшево описан в [[ADR-0003]].

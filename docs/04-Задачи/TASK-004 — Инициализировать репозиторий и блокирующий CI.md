---
title: TASK-004 — Инициализировать репозиторий и блокирующий CI
aliases: [TASK-004]
type: task
status: in-progress
priority: p1
stage: M0
created: 2026-07-28
updated: 2026-07-29
related: ["[[Конвенции разработки]]", "[[Q-005]]", "[[Q-007]]", "[[DEBT-001]]", "[[ADR-0007]]"]
tags: [task, status/in-progress, p1, stage/M0, area/process]
---

# TASK-004 — Инициализировать репозиторий и блокирующий CI

> [!info] Карточка
> **Статус:** 🟠 in-progress · **Приоритет:** p1 · **Этап:** M0
> **Пункт ТЗ:** 8 (тестирование и CI), 9 · **Связано:** [[Q-007]], [[DEBT-001]], [[ADR-0007]]

## Описание

`git init`, структура каталогов по [[Архитектура#2. Структура модулей (ТЗ разд. 9)]], `.gitignore`, линтер, форматтер, typecheck, аудит зависимостей — **всё блокирующее с самого начала**. ТЗ требует, чтобы CI блокировал мёрж при падении любой проверки; вводить это «когда будет что проверять» поздно — правило перестаёт соблюдаться в первый же спорный момент.

Сюда же: Renovate/Dependabot и запрет мажорных апдейтов без прогона контрактных тестов (ТЗ 6).

## Критерий готовности

- [x] Структура каталогов соответствует ТЗ разд. 9.
- [x] Конфигурация инструментов: `package.json`, строгий `tsconfig.json`, `eslint.config.mjs`, `.prettierrc.json`, `vitest.config.ts`.
- [x] `.gitignore` исключает `.env` и любые секреты (ТЗ разд. 6).
- [x] CI-воркфлоу написан и **падает** при ошибке линтера, формата, типов, теста или аудита зависимостей.
- [x] Автоматический апдейтер (`renovate.json`) с запретом автомёржа мажоров без контрактных тестов.
- [x] `git init -b main`, первый коммит `7e09f9a` — 74 файла.
- [x] `pnpm install`, lock-файл создан и закоммичен ([[DEBT-001]] погашен).
- [x] `pnpm run verify` прогнан локально: линтер, формат, типы, 24 теста — зелёные.
- [x] `.gitattributes` защищает канон ТЗ от нормализации переносов строк ([[ADR-0002]]); SHA-256 после коммита совпадает с исходным.
- [ ] CI прогнан на сервере — нужен удалённый репозиторий ([[Q-005]]).
- [ ] Документирован в [[Конвенции разработки]] формат веток, коммитов и чек-лист ревью.

## Открытые зависимости

- [[Q-005]] — где хостится репозиторий, кто владеет CI. Без удалённого репозитория воркфлоу не может быть прогнан ни разу.

## Лог

- `2026-07-28` — создана.
- `2026-07-29` — написана вся конфигурация репозитория и CI. Обнаружено, что на машине нет Node, pnpm и git — заведён [[Q-007]], хвост по lock-файлу зафиксирован в [[DEBT-001]]. CI намеренно оставлен с `--frozen-lockfile`: послабление «на время» переживает свою причину.
- `2026-07-29` — тулчейн установлен, зависимости поставлены, все проверки прогнаны, репозиторий инициализирован, первый коммит сделан. Версия Node в CI выровнена на 24 (Node 20 вне поддержки), `engines` подняты до `>=22.11`. Остаётся прогон CI на сервере и конвенции ветвления.

---
title: TASK-008 — Собрать чек-лист negative-требований из 40+ дефектов текущей CMS
aliases: [TASK-008]
type: task
status: cancelled
priority: p2
stage: M0
created: 2026-07-28
updated: 2026-08-03
related: ["[[ADR-0006]]", "[[Окружение платформы]]", "[[Критерии приёмки]]"]
tags: [task, status/cancelled, p2, stage/M0, area/process, area/security]
---

# TASK-008 — Чек-лист negative-требований из дефектов текущей CMS

> [!warning] Карточка
> **Статус:** ⛔ cancelled `2026-08-03` — списка не будет, заказчик сообщил, что предоставить его не может
> **Что вместо:** см. раздел «Чем заменено»

## Описание

Заказчик сообщил, что другая команда нашла в работающей на проде CMS **более 40 ошибок**, и именно это стало основанием решения о полном переписывании ([[ADR-0006]]).

Этот список — ценный материал: каждая ошибка есть доказательство того, что некоторый класс проблем **возможен** в системе такого типа. Новая архитектура обязана сделать его невозможным по конструкции, а не «учесть при разработке».

## Что сделать

- [ ] Получить перечень дефектов от заказчика.
- [ ] Классифицировать: утечка данных · нарушение изоляции тенантов · невалидная выдача · комплаенс · потеря событий · доступность · производительность · UX редактора.
- [ ] Для каждого класса сформулировать **negative-требование** — что новая система должна делать невозможным, а не просто корректно обрабатывать.
- [ ] Для каждого negative-требования указать механизм принуждения: правило доступа, схема, валидатор сборки релиза, тип в БД, ограничение в CI.
- [ ] Те, что попадают в разряд регуляторных, добавить в [[Критерии приёмки]] как проверяемые автотестом.

## Критерий готовности

- [ ] Каждый дефект из списка либо закрыт механизмом в архитектуре, либо явно помечен как неприменимый к новой модели с обоснованием.
- [ ] Ни один не остался в статусе «постараемся не повторить».

> [!warning] Почему это `p1`, а не «когда будет время»
> Три дефекта уже найдены при обследовании (fire-and-forget инвалидация, торговые условия свободным текстом, отсутствие релизной модели), и все три — архитектурные. Остальные 40+ с высокой вероятностью тоже указывают на классы проблем, а не на опечатки. Узнать их **до** проектирования модели данных дешевле, чем после.

## Чем заменено

Списка не будет — значит источник negative-требований остаётся один: **собственное обследование**. Оно уже дало три архитектурных дефекта, и все три закрыты по конструкции:

| Найденный дефект | Чем закрыт |
|---|---|
| Инвалидация через fire-and-forget HTTP без outbox | транзакционный outbox — [[ADR-0017]], реализация на M1 |
| Торговые условия свободным текстом без источника истины | [[ADR-0004]] и [[ADR-0005]]: запись со ссылкой на источник, сверка, режим `unverified` |
| Отсутствие релизной модели, каналов и отката | [[ADR-0015]] |

> [!warning] Чего мы лишились
> Три дефекта нашлись за час обследования одного контура. Сорок с лишним, найденные командой за длительную эксплуатацию, почти наверняка содержат то, чего снаружи не видно: сценарии, воспроизводящиеся раз в месяц, и грабли конкретного домена.
>
> Это **принятый риск**, а не закрытый вопрос. Компенсируем единственным доступным способом — плотностью проверок: каждое правило, которое можно проверить автотестом, проверяется, а не описывается.

Если список когда-нибудь появится, задачу стоит открыть заново: она всё ещё полезна.

## Лог

- `2026-07-28` — создана как следствие [[ADR-0006]].
- `2026-08-03` — заказчик сообщил, что списка не будет. Отменена; источником negative-требований остаётся собственное обследование, риск принят и записан.

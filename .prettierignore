# Волт документации форматируется Obsidian, а не Prettier:
# он ломает callouts, wikilinks и таблицы, выравненные вручную.
docs/

node_modules/
dist/
.next/
coverage/
pnpm-lock.yaml

# Генерируется `payload generate:importmap`
src/app/(payload)/admin/importMap.js

# Генерируется `payload generate:types`. Форматировать нельзя: CI сверяет файл
# с результатом генерации, а prettier снимает точки с запятой — сверка начнёт
# падать на каждом прогоне.
src/payload-types.ts

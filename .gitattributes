# Единый перевод строки в репозитории — иначе diff на смешанной команде
# (Windows + Linux CI) шумит на каждом файле.
* text=auto eol=lf

# ─────────────────────────────────────────────────────────────────────────────
# ИСКЛЮЧЕНИЕ: канон ТЗ хранится побайтово, как получен (ADR-0002).
# Нормализация переносов строк изменила бы файл и сломала бы проверку
# целостности по SHA-256 — то есть ровно то свойство, ради которого канон
# и заведён. `-text` запрещает git любую конверсию содержимого.
# ─────────────────────────────────────────────────────────────────────────────
docs/00-source/PROMPT-ORIGINAL.md -text

# Lock-файл не участвует в ручных слияниях: конфликт разрешается
# повторным `pnpm install`, а не редактированием.
pnpm-lock.yaml -diff linguist-generated=true
